# Image-Classification

#### TO classify images into Buildings' 'Forest' 'Glacier' 'Mountain' 'Sea' 'Street' using tensorflow-keras
#### build a model from scratch then use pretrained model(VGG16) to improve performance
